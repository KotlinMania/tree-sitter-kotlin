# Port targets — `tree-sitter-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `tree-sitter` that codex-rs uses, and therefore the minimum subset that `tree-sitter-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-171] (rank `102`, tier `direct`, status `To Do`)

**Cargo package name**: `tree-sitter`
**Rust module name (in `use` statements)**: `tree_sitter`

## Summary

- **7** distinct symbols imported by codex-rs
- **8** total `use` statements
- **2** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `tree_sitter::Parser`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-apply-patch`, `codex-shell-command`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `apply-patch/src/invocation.rs:7` (`codex-apply-patch`) — `use tree_sitter::Parser`
- `shell-command/src/bash.rs:4` (`codex-shell-command`) — `use tree_sitter::Parser`

### `tree_sitter::Query`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-apply-patch`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `apply-patch/src/invocation.rs:8` (`codex-apply-patch`) — `use tree_sitter::Query`

### `tree_sitter::QueryCursor`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-apply-patch`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `apply-patch/src/invocation.rs:9` (`codex-apply-patch`) — `use tree_sitter::QueryCursor`

### `tree_sitter::StreamingIterator`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-apply-patch`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `apply-patch/src/invocation.rs:10` (`codex-apply-patch`) — `use tree_sitter::StreamingIterator`

### `tree_sitter::LanguageError`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-apply-patch`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `apply-patch/src/invocation.rs:25` (`codex-apply-patch`) — `use tree_sitter::LanguageError`

### `tree_sitter::Node`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-command`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-command/src/bash.rs:2` (`codex-shell-command`) — `use tree_sitter::Node`

### `tree_sitter::Tree`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-command`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-command/src/bash.rs:5` (`codex-shell-command`) — `use tree_sitter::Tree`
