# Rust callers — `tree-sitter-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `tree-sitter` | `tree_sitter` | 8 | 2 | 1 | 8 |

## `tree-sitter`

### Inbound symbols (7 distinct, 8 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `tree-sitter`.

#### `tree_sitter::Parser` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-apply-patch` `src/invocation.rs:7` — `use tree_sitter::Parser`
- ✅ `codex-kotlin/codex-shell-command` `src/bash.rs:4` — `use tree_sitter::Parser`

#### `tree_sitter::LanguageError` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-apply-patch` `src/invocation.rs:25` — `use tree_sitter::LanguageError`

#### `tree_sitter::Node` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-shell-command` `src/bash.rs:2` — `use tree_sitter::Node`

#### `tree_sitter::Query` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-apply-patch` `src/invocation.rs:8` — `use tree_sitter::Query`

#### `tree_sitter::QueryCursor` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-apply-patch` `src/invocation.rs:9` — `use tree_sitter::QueryCursor`

#### `tree_sitter::StreamingIterator` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-apply-patch` `src/invocation.rs:10` — `use tree_sitter::StreamingIterator`

#### `tree_sitter::Tree` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-shell-command` `src/bash.rs:5` — `use tree_sitter::Tree`
