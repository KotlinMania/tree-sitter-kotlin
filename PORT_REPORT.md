# Port report — `tree-sitter-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `tree-sitter`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 8 `use` sites from 1 other repos
- **Outbound**: 0 `use` sites to 0 other repos
- **Unresolved external**: 4 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `tree-sitter` | `tree_sitter` | 8 | 0 | COD-171 | 102 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (8 edges, 2 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-apply-patch` | 5 | ✅ |
| `codex-kotlin` | `codex-shell-command` | 3 | ✅ |

### Symbol-level demand (7 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `tree_sitter::Parser` | 2 | 2 | 0 | 2 |
| `tree_sitter::LanguageError` | 1 | 1 | 0 | 1 |
| `tree_sitter::Node` | 1 | 1 | 0 | 1 |
| `tree_sitter::Query` | 1 | 1 | 0 | 1 |
| `tree_sitter::QueryCursor` | 1 | 1 | 0 | 1 |
| `tree_sitter::StreamingIterator` | 1 | 1 | 0 | 1 |
| `tree_sitter::Tree` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 7 symbols)

##### `tree_sitter::Parser` (2 sites, 2 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-apply-patch` `src/invocation.rs:7` — `use tree_sitter::Parser`
- `codex-kotlin/codex-shell-command` `src/bash.rs:4` — `use tree_sitter::Parser`

##### `tree_sitter::LanguageError` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-apply-patch` `src/invocation.rs:25` — `use tree_sitter::LanguageError`

##### `tree_sitter::Node` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-command` `src/bash.rs:2` — `use tree_sitter::Node`

##### `tree_sitter::Query` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-apply-patch` `src/invocation.rs:8` — `use tree_sitter::Query`

##### `tree_sitter::QueryCursor` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-apply-patch` `src/invocation.rs:9` — `use tree_sitter::QueryCursor`

##### `tree_sitter::StreamingIterator` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-apply-patch` `src/invocation.rs:10` — `use tree_sitter::StreamingIterator`

##### `tree_sitter::Tree` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-command` `src/bash.rs:5` — `use tree_sitter::Tree`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

_No outbound edges to other workspace repos._

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `bindgen` | 8 | 5 | `tree-sitter` |
| `streaming-iterator` | 2 | 2 | `tree-sitter` |
| `tree-sitter-language` | 1 | 1 | `tree-sitter` |
| `wasmtime-c-api-impl` | 1 | 1 | `tree-sitter` |
